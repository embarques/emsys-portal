export const API_ENDPOINTS = {
  AUTH_TOKEN: "/auth/token",
  USER_PERMISSIONS: "/users/permissions",
  USERS: "/users",
  EMPLOYEES: "/employees",
  CUSTOMERS: "/customers",
  BRANCHES: "/branches",
} as const;
